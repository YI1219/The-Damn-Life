// Release script — placeholder for future implementation.
// Will handle: version bump, changelog generation, git tag, publish.

console.log('⚠️  Release script is not yet implemented.')
console.log('Planned: version bump → changelog → git tag → publish')
process.exit(0)
