# Execution run logs

Each development or integration batch uses a dated folder:

`YYYY-MM-DD_<short-topic>/`

Required in each folder:

- **RUNLOG.md** — goals, files touched, gate/smoke results, open questions.
- Optional: **decisions.md** — tool or license notes.

Start a new folder before substantial work; append **RUNLOG.md** before closing the batch.
